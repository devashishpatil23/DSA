


16: Can you write a JavaScript function to convert a given number of minutes into hours and minutes?
17: Can you write a JavaScript function to generate a random password of a specified length?
18: Can you write a JavaScript function to convert an RGB color to its hexadecimal representation?


function convertToUpperCase(arr) {
  return arr.map(str => str.toUpperCase());
}
Example usage:
javascript
Copy
Edit
const strings = ["hello", "world", "javascript"];
const uppercased = convertToUpperCase(strings);
console.log(uppercased); // Output: ["HELLO", "WORLD", "JAVASCRIPT"]